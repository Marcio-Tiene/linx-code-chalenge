{
  "name": "linx-code-chalenge",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "dev": "nodemon -w  src --exec \"babel-node src --presets env\" ",
    "build": "rimraf dist && babel src -s -D -d dist --presets env",
    "start": "node dist"
  },
  "dependencies": {
    "express": "^4.17.1",
    "nunjucks": "^3.2.2"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.7.0",
    "eslint": "^7.15.0",
    "nodemon": "^2.0.6",
    "rimraf": "^3.0.2"
  }
}
